info:
  name: Get Student FRS
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/frs/1
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code harus 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
